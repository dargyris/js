var firstName = "John";
var age = 28;

// Type coersion demonstraton
console.log( firstName + " " + age );

var job, isMarried;
job = "Teacher";
isMarried = false;

console.log( firstName + ' is a ' + age + ' year old ' + job + '.' );
console.log( 'Is he married? ' + isMarried );

// Variable mutation demonstraton
age = "twenty eight";
console.log( "\n" + age );

job = 'driver';

var lastName = prompt( 'What is his last name?' );
console.log( lastName );
